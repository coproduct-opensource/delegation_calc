/-
§4.4 — Protocol-Logic Correspondence theorem.

The bridge between DLC proof terms and Tamarin protocol traces. Every Tamarin
trace `tr` projects to a DLC `(Γ, M, φ)` with `Γ ⊢ M : φ`, and every DLC
derivation lifts to a Tamarin trace.

Composes L2.3 (wire ↔ symbolic encoding) and L2.5 (trace-to-derivation lifting)
to give T2 its symbolic half. Lands at M2.M15.
-/

import DLC.Correspondence

namespace DLC

/-- Placeholder. M2.M15 closes the §4.4 correspondence theorem. -/
theorem protocol_correspondence_stub : True := trivial

end DLC
